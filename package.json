{
  "name": "vtj",
  "private": true,
  "description": "VTJ 一款基于 Vue3 + Typescript 的低代码开发工具，内置了设计器引擎、渲染器和代码生成器。",
  "type": "module",
  "devDependencies": {
    "pnpm": "~8.11.0",
    "lerna": "~8.0.0",
    "axios": "~1.6.2",
    "fs-extra": "~11.2.0",
    "jsdom": "~23.0.1",
    "typedoc": "~0.25.4"
  },
  "scripts": {
    "setup": "pnpm install --unsafe-perm --registry=https://registry.npmmirror.com",
    "dev": "npm run copy && cd dev && npm run dev",
    "dev:build": "cd dev && npm run build",
    "dev:preview": "cd dev && npm run preview",
    "clean": "lerna clean -y && node scripts/clean.mjs",
    "copy": "node scripts/copy.mjs",
    "reset": "pnpm run clean && pnpm run setup",
    "outdate": "pnpm outdated -r",
    "update": "pnpm up -r --unsafe-perm --registry=https://registry.npmmirror.com",
    "sync": "node scripts/sync.mjs",
    "test": "lerna run --no-private test",
    "build": "lerna run --no-private build",
    "coverage": "lerna run --no-private coverage",
    "typedoc": "typedoc",
    "ci": "git add . && git commit -m \"ci commit \" && git push",
    "prerelease": "npm run setup && lerna version prerelease --yes && npm run test && npm run build && npm run ci && lerna publish from-package --dist-tag test --no-private --yes && pnpm run sync",
    "patch": "npm run setup && lerna version patch --yes && npm run build && npm run ci && lerna publish from-package --force-publish --no-private --yes && pnpm run sync",
    "minor": "npm run setup && lerna version minor --yes && npm run build && npm run ci && lerna publish from-package --force-publish --no-private --yes && pnpm run sync",
    "test:core": "cd packages/core && npm run vitest",
    "test:designer": "cd packages/designer && npm run vitest",
    "test:renderer": "cd packages/renderer && npm run vitest",
    "docs:dev": "cd docs && vitepress dev",
    "docs:build": "cd docs && vitepress build && cd .. && npm run typedoc",
    "docs:preview": "cd docs && vitepress preview"
  },
  "engines": {
    "node": ">=20.0.0",
    "lerna": ">=8.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "^pnpm@8.10.4",
  "publishConfig": {
    "access": "public"
  }
}
