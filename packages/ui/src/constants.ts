export const INSTALLED_KEY = Symbol('INSTALLED_KEY');

export const IS_DRAGGING_CLASS_NAME = 'is-dragging';

export const IS_RESIZING_CLASS_NAME = 'is-resizing';

export type ButtonSize = '' | 'default' | 'small' | 'large';
