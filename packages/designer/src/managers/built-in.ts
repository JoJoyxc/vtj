import { widgets } from '../components';
import { type Widget } from '../framework';

export const builtInWidgets: Widget[] = [
  {
    name: 'Logo',
    region: 'Brand',
    component: widgets.Logo
  }
];
