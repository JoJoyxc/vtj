import type { MaterialDescription } from '@vtj/core';

const desc: MaterialDescription = {
  name: 'Button',
  label: '按钮',
  categoryId: 'form',
  props: [],
  snippet: {
    children: 'Button'
  }
};

export default desc;
